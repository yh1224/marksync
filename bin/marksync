#!/bin/sh
MARKSYNC_HOME="$( cd "$( dirname "$0" )" && cd .. && pwd )"
APP_JAR="${MARKSYNC_HOME}"/target/scala-2.13/marksync-assembly-0.1.jar

if [ "$1" = "--build" ]; then
  (cd "${MARKSYNC_HOME}" && sbt assembly) || exit $?
  shift
  test $# -eq 0 && exit 0
fi

scala "${APP_JAR}" $*
